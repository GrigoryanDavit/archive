import { ReactComponent as AnimatedLoaderIcon } from '../icons/loader.svg';
import { StyledLoaderContainer } from './styled';

const Loader = () => (
  <StyledLoaderContainer>
    <AnimatedLoaderIcon />
  </StyledLoaderContainer>
);

export default Loader;
